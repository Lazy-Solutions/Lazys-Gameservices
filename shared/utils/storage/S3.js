export async function S3Storage ({ bucket })
{


    function add (path, value) { };
    function remove (path) { };
    function hasKey (path) { };
    function getData (path) { };
    function createBucket () { };
    function checkBucketExist () { };

    return {
        add,
        remove,
        getData,
        hasKey,
    };
}